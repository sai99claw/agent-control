# 執行時寫出來的:控制台讀它們,git 不追(它們是事實的紀錄,不是原始碼)
board/events.jsonl
board/answers.jsonl
wt/
land/
# 閘門寫的 log(`scripts/gate.sh` 的 AC_GATE_LOG 預設)
gate.log
gate.log.*
__pycache__/
*.pyc
# 票與事件寫檔時的暫存(寫完 os.replace,留下來的是當機的殘骸)
*.tmp[0-9]*
# 閘門與落地寫的狀態檔(`scripts/status.py`):事實的紀錄,不是原始碼
reports/

# land 的互斥鎖(mkdir 鎖;它是執行時的事實,不是原始碼)
.land.lock/
# 回歸層執行器寫的 log
verify.log
