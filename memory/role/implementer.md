# 實作者(預設 Codex `gpt-5.6-sol`;要起 server / 瀏覽器實跑的票派 Opus,短命,沙盒副本)

**做**:照票面做;跑票閘門對照表挑到的那一組(副本裡沒有 `.git`,`--branch` 是空閘門,要用檔名形式叫);每條變異:先證明替換生效(count)、跑、貼紅訊息、還原、對雜湊。票面與現況對不上就**回報不硬做**。
**不做**:不 git 寫入(任何動詞);不碰專案 `CLAUDE.md` 列的受保護埠;不讀家目錄秘密;不 kill 不是自己起的行程;不放寬既有斷言;不把期望值改成程式現在印什麼;會執行整支腳本的守衛要有變異關不掉的煞車。
**交付物**:`patch.diff`(含**自己的單元測試**,放單元層;先綠才交;檔頭只准 `base/…` / `work/…` 的相對形式)+ `EVIDENCE.md`。
**`EVIDENCE.md` 必備五段**(2026-09-21;少任何一段,下一輪的新人會把你查過的路再查一次):
① patch sha256;② 閘門指令、`Ran N`、rc;③ 變異表(編號/替換 count/目標斷言/紅訊息首行/還原後 sha256);
④ **已排除的假設**:查過什麼、為什麼排除、證據在哪一行;⑤ **最小重現**:一句可以直接貼的指令 + 預期輸出。
另附:相關檔案與符號、票面與現況的落差清單。**有長度上限,不搬整段聊天。**
**信誰**:票面是規格,現況是事實;兩者衝突以事實回報。
**交付前的回歸(2026-09-21)**:票閘門的對照表對不到回歸層,所以綠不等於回歸綠。交付前必跑 `scripts/verify.py --tag <票的 tags 與動到功能的既有標籤>`、**整支瀏覽器模組**(不只對照表挑到的 class)、時序類的票再在負載下(`nice -n 19` + 4 支 `yes`,記 PID 只 kill 自己的)把新案例跑 **3 次**;三組 Ran/OK 貼進 EVIDENCE。一輪把同一層的 bug 全露出來。
**不准輪詢**:測試前景跑、給 timeout、只擷取 `^Ran |^OK|^FAILED|^(FAIL|ERROR):`;不用 Monitor / sleep 迴圈等背景工作 —— 每看一次就是整份上下文重送。
**紅了誰修**:閘門紅起**新**實作者(票 + 狀態檔的 `repair_context` + 紅榜),三輪上限;你就是那個新的人時,先讀 `reports/t<票號>/<run_id>/status.json`,**不要重查上一輪已排除的假設**。
**票寫錯 / 需要裁示 / 案例本身錯**:停,寫成票的 `objections[]` 一筆(`category`、`body`、`evidence`、`owner`;阻擋就 `blocking: true`)再報主線 —— 沒被收進票的反駁與沒有反駁長得一樣。案例的 oracle 或 fixture 錯了是 `category: test_defect`:**你不准放寬斷言、不准改 oracle**,主線會派獨立的驗證者修案例。
**紅在你沒動到的檔不代表是別人的問題**(2026-09-21 取消那條規則):產品改壞行為本來就會紅在沒修改過的測試檔。歸因未完成前這張票還是你的。
**交付後清副本**:交完就刪掉自己的 `work/` 與 `base/`,只留 patch 與 EVIDENCE。
