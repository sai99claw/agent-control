---
topic: 整理 memory/role/implementer.md 與它的 inbox(#52)
kind: memory-consolidation
parties: [opus / consolidator, fable / consolidator]
stop:
  max_rounds: 3
  budget: "40K token / 每位"
started: 2026-09-27T03:50+08:00
---

## 背景
- 票 #52(`memory.py check` 自動開;票主旨寫「23 字元」,實際是 inbox **24 / 20 行**超標)。
- 實測(`python3 scripts/memory.py check`,2026-09-27):`implementer.md 1675 / 2000`(主檔未超)、
  `implementer.inbox.md 24 / 20 行超過`。所以這張票真正要處理的是**把 inbox 清空併進主檔,而主檔只剩 ~325 字元空間**。
- 主檔沒有 front matter(`cap_chars` 走 config 預設 2000,`cap_history` 不存在)。
- 規則包已經注入的東西(實測 `python3 scripts/rules.py pack implementer --model opus`):
  第 2 行固定 D-016 效率句;`## 結構化交付(D-017)` 整段(result 塊的鍵、留空不編)。
- 路由參考:`memory/project/sandbox.md` 已有一條 `control_harness.py` 的 SCRIPT_FILES 規矩(#46)。
  本票 `allowed_write_paths` 只有 implementer.md 與它的 inbox,**寫不到 project/**。

## 第 1 輪 — opus

### inbox 逐條(行號 = `implementer.inbox.md` 行號)

| 行 | 內容摘要 | 判 | 為什麼 | 標記 |
|---|---|---|---|---|
| 1 | `verify/**` 是驗證者的,實作者只寫 demo/ 層 | **併**進「不做」 | 是行為邊界,不是案例;一句就夠 | 2026-09-22 / #630 / 實測(落地時撞檔) |
| 2 | 「第一條原則 (#1)」 | **刪** | 測試 `memory.py note` 留下的垃圾行,無內容 | 2026-09-22 / #1 / 實測(讀原文) |
| 3 | 「仍會收這行 (#1)」 | **刪** | 同上 | 同上 |
| 4 | 重現對不上紅榜先 `env \| grep ^AC_` | **併**成新的「副本環境」一條(與 24、16、21、26 合) | 同一類:派工環境/副本跟主 repo 不一樣 | 2026-09-22 / #7 / 實測 |
| 5 | D-016 效率句 | **刪** | 規則包第 2 行固定注入,每個實作者都已讀到;主檔再寫一次是重複付費 | 2026-09-22 / D-016 / 實測(pack 輸出第 2 行;`rules.py` 67–70 行) |
| 6 | 移植舊 patch 不信 diff3 自動合併,逐檔套、手解 rej、反向核對 | **留**(壓短,放進「測試與 patch 寫法」一條) | 修復輪(新 worker 接舊 patch)一定會遇到;靜默掉行是最貴的那種錯 | 2026-09-22 / #13 / 實測 |
| 7 | session 失效案例只改自己分頁的 token,不打 logout-all | **併**(抽象成原則) | 原文是 tabby 專案細節(`test_browsers.GUESTS`);留下的是原則「破壞性動作只動自己擁有的狀態,不撤共用夾具」 | 2026-09-22 / #642 / 實測;原作者是 verifier |
| 8 | 時序案例窗口 150ms→600ms | **併**進「交付前的回歸」一條的時序子句(一句) | 數字是 tabby 專案的,原則是「窗口要蓋得住負載下的 parse/exec,前提不成立要大聲紅」;若空間不夠第一個讓出來(原作者是 verifier,驗證者記憶較合適) | 2026-09-22 / #642(比照 #639)/ 實測 |
| 9 | D-018 設計為未來 token 打算,不繞路 | **併**進「票寫錯/需要裁示」一條 | 對實作者的可操作部分只有「太複雜提 objection 開票,不准改名/加第二欄位/各做各的形狀」;前半段(取捨看未來每票)是設計者/主線的事,`design.md` 已有 | 2026-09-23 / D-018 / 引用 |
| 10 | patch 檔頭只准 `base/…` `work/…` | **刪** | 主檔「交付物」一條已寫「檔頭只准 `base/…` / `work/…` 的相對形式」,重複 | 2026-09-23 / #25 / 實測 |
| 11 | 副本無 `.git`,`test_no_project_names` 必紅,拿 base 副本對照 | **併**(與 21 合) | 21 是 11 的完整版(多了 `cp -R work + git init` 的做法) | 2026-09-23 / #28 / 實測 |
| 12 | 測試前景跑加 timeout;丟背景後收回合 = 沒交 | **併**進「不准輪詢」一條(加半句) | 主檔已有「前景跑、給 timeout」;inbox 新增的是**後果**(`claude -p` 不會再醒),半句值得留 | 2026-09-23 / #26 / 實測 |
| 13 | 給共用夾具加預設值會關掉受測層,要反向檢查 | **留**(壓短) | 「為了回綠改夾具」是放寬斷言的變形,主檔「不放寬既有斷言」沒涵蓋到夾具 | 2026-09-23 / #27 / 實測 |
| 14,15,17,19 | 守衛量那一段不量整份輸出(**同一句寫了 4 次**) | **留 1 併 3** | 變異測試是實作者核心動作;四次重複本身就是證據:同一個 worker 反覆踩 | 2026-09-23 / #29 / 實測 |
| 16 | gate.log、`*.env-suspect.json` 被 `diff -ruN` 收進 patch;交付前 `diff -rq base work \| grep '^Only in work'` | **併**進「副本環境」 | 副本衛生同一類 | 2026-09-23 / #29 / 實測 |
| 18,20 | base 跑對照長 `__pycache__`(**重複 2 次**) | **留 1 併 1**,放「副本環境」 | 「base 一字不動」是對照證據成立的前提 | 2026-09-26 / #40 / 實測 |
| 21 | 無 `.git` 全套要在 `cp -R work + git init` 複本跑,base 複本對照 | **併**(吸收 11) | 見 11 | 2026-09-26 / #49 / 實測 |
| 22 | 新增會起 headless 模型的腳本,先在 `control_harness.py` 放替身命令 | **刪(移到 project/sandbox.md)** | 是 agent-control repo 的**專案事實**,不是實作者角色的準則;`sandbox.md` 已有同一支檔的 #46 規矩,放在一起才找得到。本票寫不到 project/,請主線另用 `memory.py note project sandbox` 補(或開票) | 2026-09-26 / #42 / 實測 |
| 23 | 證「兩件事同時在跑」要雙向會合 | **留**(壓短) | 並行測試的思考方式,跨專案成立 | 2026-09-27 / #51 / 實測 |
| 24 | 副本外自建 git 跑 gate 前 `env -u AC_ROOT -u AC_TICKET` | **併**進「副本環境」 | 與 4 同一類;後果嚴重(worker 改到主線的票) | 2026-09-27 / #44 / 實測 |

計數(inbox 24 行):**留 4**(6、13、23、14 的那一句)、**併 12**(1、4、7、8、9、11、12、16、18、21、24 + 重複行 15/17/19/20 算進「併 3+1」)、**刪 5**(2、3、5、10、22)。
換成「條目」算(去重後 18 條不同內容):留 4 / 併 9 / 刪 5。

### 主檔(implementer.md)逐條

| 條 | 判 | 為什麼 |
|---|---|---|
| 標題、做、不做、交付物、EVIDENCE 五段、信誰、交付前的回歸、不准輪詢、紅了誰修、票寫錯、紅在沒動到的檔、交付後清副本 | **留** | 都是行為準則,不是案例;沒有跟其他記憶檔重複 |
| 「再加一塊 `result`(2026-09-22)」 | **併成一句指路** | 規則包 `## 結構化交付(D-017)` 整段已經注入(實測,見背景);主檔只需「見規則包 D-017 段,留空不編」。省 ~150 字元 |

**主檔的缺口(要對方看)**:主檔 13 條多半只有日期,**沒有來源票號、沒有實測/推論標記**。
票的驗收要求「保留的每一條仍帶三個標記」—— 補齊每條約多 15–25 字元,13 條 ≈ 200–300 字元。
我不知道每條的來源票(git log 看得到 D-017 / 82a8b1d / db32c07 等 commit,但沒逐條對過),**這一格需要對方或主線補證據**,不要猜。

### 合併後的草稿(新增/改寫的部分;其餘主檔原文不動)

```
**不做**(尾巴加):不寫 `verify/**`(驗證者的,同名檔落地會打架;#630,2026-09-22,實測)。
**`result` 塊**:見規則包 D-017 段;寫不出的欄位留空不編(2026-09-22,D-017,引用)。
**不准輪詢**(尾巴加):測試丟背景就收回合 = 沒交,`claude -p` 不會再醒(#26,2026-09-23,實測)。
**票寫錯**(尾巴加):太複雜就提 objection 開票,不繞路(改名、加第二個欄位、各做各的形狀)(D-018,2026-09-23,引用)。
**回歸**(時序子句加):窗口要蓋得住負載下的 parse/exec,前提不成立要大聲紅(#642,2026-09-22,實測)。
**副本環境**(2026-09-22~27,實測):派工帶著 `AC_*` —— 重現對不上紅榜先 `env | grep ^AC_`(#7);副本外自建 git 跑 gate 前 `env -u AC_ROOT -u AC_TICKET`,否則寫回主 repo(#44)。副本無 `.git`,`test_no_project_names` 必紅:全套在 `cp -R work`+`git init` 的複本跑,用 base 複本同條對照(#28/#49)。base 一字不動:對照趟加 `PYTHONDONTWRITEBYTECODE=1`(#40);交付前 `diff -rq base work | grep '^Only in work'`(gate.log 會混進 patch,#29)。
**測試與 patch 的寫法**(2026-09-22~27,實測):守衛量那一段,不量整份輸出(#29);證並行要雙向會合,各寫各的標記、各等對方(#51);為回綠給共用夾具加預設值前,反向查它會不會關掉受測層(#27);破壞性動作只動自己擁有的狀態,不撤共用夾具(#642);移植舊 patch 逐檔套、手解 rej、以 diff(舊 work, 新 work) 反向核對上游 delta(#13)。
```

**字元預算(實測,見下方量法)**:見「量測」一節。結論先寫:併完一定超過 2000;
我的主張是 **提高上限到 2700**,而不是再砍原則。

### 量測(實測,2026-09-27)
用 python 量:主檔 1675 字元;`result` 那條 162;上面草稿塊 889(含「(尾巴加)」之類標籤約 30)。
估計合併後 = 1675 − 162 + 889 ≈ **2400**(尚未替主檔 12 條補三標記)。
補三標記再加 ≈ 200–300 → **約 2600–2700**。

### 主張
- 主張:inbox 全部清空;5 條刪、9 條併、4 條留;主檔 `result` 那條縮成指路;**上限 2000 → 2700**,
  `cap_history` 理由:「多讀的 ~700 字元是副本環境與測試寫法兩條,各自對應一次整輪重跑
  (#44 寫回主 repo、#26 patch 沒生出來、#29 變異存活、#13 靜默掉行);一輪重跑的成本是整份上下文重送,遠大於每次多讀 700 字元」。
- 證據等級:實測(`memory.py check` 輸出、`rules.py pack implementer` 輸出、inbox 原文的重複次數)。
- 什麼會讓我改變主意:
  1. 對方示範把「副本環境」一條移到 `memory/project/sandbox.md`(那是專案事實,不是角色準則)並論證實作者的規則包會帶到它 —— 那我同意不提上限,改成開一張路由票;
  2. 對方指出任一條「留」其實已經在 `docs/DISPATCH-TEMPLATE.md` 被規則包注入的那幾節(§1/§3/§5)講過 —— 那條我改判刪;
  3. 對方補得出主檔 13 條的來源票號,且量出補完三標記後仍 ≤ 2000 —— 我撤回提高上限。

### 我看不懂、先不刪、問對方的
- inbox 7、8(#642,verifier@opus 寫進 implementer inbox):原作者是驗證者,放在實作者記憶是寫錯層,還是故意(實作者也寫 demo/ 層的瀏覽器測試)?我判「抽象成原則後併」,但若對方認為該整條搬去 verifier 記憶,我不堅持。

## 第 2 輪 — opus(讀 `discussions/2026-09-27-memory-fable.md` 之後)

**先更正自己**:第 1 輪表格的 inbox 行號有錯,fable 的才對(實測 `grep -n`):#29 M18 在 14/15/16/18,
gate.log 在 17,`__pycache__` 在 19/20,#42 在 21,#49 在 22。內容判定不受影響。

**主線裁示(本輪起算)**:(a) 上限最多 2700,分歧時以「之後每票多讀多少、少重來幾輪」判;
(b) #42、#642 要搬去 project 層的原文列在結論,主線用 `memory.py note` 補,不擋本票;
(c) 主檔來源票號對不到的標「?」,不編。

### 逐條:分歧與同意

| # | 項目 | fable | opus(第 1 輪) | 本輪回應 | 狀態 |
|---|---|---|---|---|---|
| D1 | inbox 7 #642 session 失效只改 token | 刪,原文移 tabby 專案層 | 抽象成「破壞性動作只動自己擁有的狀態」併入 | **改判同意 fable**:抽象後的原則已被主檔「不做」的「不 kill 不是自己起的行程」大致蓋住,多寫一句每票多讀 ~40 字、救不回任何一輪;原文依裁示 (b) 搬 project 層 | 已收斂 |
| D2 | inbox 21 #42 headless 替身 | 留(縮),但說不反對移 project | 刪,移 `project/sandbox.md` | 依裁示 (b) 搬 project 層;fable 第 1 輪已預先同意。判準:它只在「新增會起模型的 agent-control 腳本」那種票用得到,每張實作票都讀是白付,放 sandbox.md 與 #46 同檔反而找得到 | 已收斂 |
| D3 | 上限 | 2500(推論) | 2700(實測估) | 兩個數字差在**我算了主檔補三標記(每條 15–25 字)、fable 沒算**,而 fable 的主檔結構壓縮(D4–D7)我沒算。兩者相抵,誰的數字對要量合併後的實際長度才知道。**提案**:第三段合併後實量,`cap = 實量 + ≤100 餘裕,取整到百,且 ≤ 2700`;實量 ≤ 2400 就用 fable 的 2500。`cap_history` 理由兩邊一致(省的是一輪 auto-fix:#26/#29/#40/#44 各因缺一條多跑一輪),只是雙方都**沒量過一輪 auto-fix 的 token**,那格標推論 | **未收斂**(規則已提,數字等第三段實量) |
| D4 | 主檔 L1 標題(派哪個模型) | 縮:派模型是派工者的判斷 | 留 | **同意 fable**,理由:實作者讀了也不會改變任何行為;模型路由在 config 與主線記憶;省 ~80 字 | 已收斂 |
| D5 | L11 信誰 | 併進 L3「回報不硬做」 | 留 | **同意**,理由:同一判準寫兩次 | 已收斂 |
| D6 | L16 紅在沒動到的檔 | 併進 L14 紅了誰修 | 留 | **同意**,理由:同一主題「紅的歸屬」 | 已收斂 |
| D7 | L10 另附 / 長度上限 | 併進交付物 | 留 | **同意**,理由:同屬交付格式 | 已收斂 |
| D8 | L9 result 塊 | 併進交付物 | 縮成指路規則包 D-017 段 | 形狀相容:併進交付物,只留半句「+ `## result` 塊(規則包 D-017 段;寫不出留空不編)」;鍵與範例不重抄(實測 pack implementer 已帶整段) | 已收斂 |
| D9 | inbox 10 #25 patch 檔頭 | 併(補「在 $W 裡 `diff -ruN base work`」) | 刪(主檔已有) | **改判同意 fable**。實測:`git log -S "diff -ruN"` 顯示範本 2026-09-12 起就有這個做法,#25 開在 09-23 —— 範本有寫還是出錯,所以做法值得在角色卡多 ~15 字 | 已收斂 |
| D10 | inbox 13 #27、14–18 #29 守衛 的位置 | 併進 L4「不做」 | 留,另開「測試與 patch 寫法」一段 | **同意 fable 的位置**(不開新標題,省字);只剩 #13、#51 兩條沒有現成的家,合成一句短段 | 已收斂(只是標籤「留/併」不同,內容都進主檔) |
| D11 | 副本相關的段落切法 | #28/#49 併 L3;L17+#29 gate.log+#40 成「副本衛生」;#7+#44 合(沒說放哪) | 全部合成「副本環境」一段 | **同意 fable 的切法**;#7+#44(AC_*)建議放進「副本衛生」同段,因為都是「副本與主 repo 的邊界」。由第三段合併者定 | 已收斂 |
| D12 | 主檔各條的三標記 | 第 1 輪沒提 | 補齊,缺來源的問對方 | 依裁示 (c):對不到的標「?」。請第三段合併者每段尾巴帶「(日期,來源或 ?,實測/推論/引用)」,並把這些字元算進 D3 的實量 | 已收斂(照裁示) |

**沒有分歧、同意的**(理由各一句):
- inbox 2、3(#1)刪:同意,理由是測試殘渣、無內容。
- inbox 5(D-016)刪:同意,理由是規則包第 2 行固定注入(雙方都實測)。
- inbox 1(#630)併:同意 fable 放進交付物,理由是它講的是「你交哪一層的測試」。
- inbox 4+24(#7/#44 AC_*)併:同意,理由是同一件事的兩面。
- inbox 6(#13)留縮:同意,理由是修復輪一定遇到、靜默掉行最貴。
- inbox 8(#642 時序窗口)併進 L12:同意,理由是原則跨專案成立,150/600ms 用票號指路。
- inbox 9(D-018)併進 objections:同意,理由是「繞路」正是該寫 objection 的時刻。
- inbox 11+22(#28/#49)併:同意,理由是 #49 是 #28 的完整版。
- inbox 12(#26)併進不准輪詢:同意,理由是補的是後果不是新規則。
- inbox 17、19/20(#29 gate.log、#40)併進副本衛生:同意。
- inbox 23(#51)留縮:同意,理由是並行測試的思考方式、跨專案。
- 主檔 L3、L4、L5、L6–8、L12、L13、L14、L15 留:同意。

- 主張:照上表收斂後的形狀合併;只剩 D3 上限數字,依實量決定(≤ 2700)。
- 證據等級:實測(`grep -n` 行號、`git log -S` 範本日期、第 1 輪的 `memory.py check` / `rules.py pack` / 字元估算)。
- 什麼會讓我改變主意:第三段實量 ≤ 2400 → 我接受 2500;實量 > 2600 → fable 應接受 2700 或再削 EVIDENCE 措辭(削哪句要指名)。

## 結論
- 結論:inbox 全清;刪 #1×2、D-016、#642 session 那條(搬 project)、#42(搬 project);其餘併進主檔既有段落,
  #13 與 #51 合一短段;主檔 L1 縮、L9/L10 併交付物、L11 併 L3、L16 併 L14、L17 成副本衛生;上限依實量,≤ 2700。
- 採用的證據:`memory.py check`(1675/2000、24/20 行)、`rules.py pack implementer`(D-016 第 2 行、D-017 整段已帶)、
  `git log -S "diff -ruN"`(範本 09-12 已有做法,#25 09-23 仍出錯)、inbox 原文的重複次數(#29×4、#40×2)。
- 保留的分歧:D3 上限數字 —— fable 2500(推論)、opus 2700(估算含三標記);雙方同意由第三段實量決定,不硬合。
- 產出:implementer.md 新版 + 上限變更(2000 → 實量決定,≤ 2700)由 fable 第三段跑 consolidate。
  **要主線用 `memory.py note` 搬去 project 層的原文**(裁示 b,不擋本票):
  1. → agent-control `memory/project/sandbox.md`(與 #46 同檔):
     「新增會起 headless 模型的腳本(讀 board/config.json 的 *.command)時,先在 tests/control_harness.py 的 DEFAULT_CONFIG 放一支替身命令,再把腳本加進 SCRIPT_FILES —— 缺那一段就退回真的 claude -p,既有的綠路測試會真的起模型 (#42) (#42, 2026-09-26, worker@opus)」
  2. → tabby_pool 專案層記憶(瀏覽器測試):
     「需要讓 session 失效的瀏覽器案例,只改這個分頁 localStorage 上那把 token(加一個字元);不要打 logout/logout-all —— 那會撤掉 test_browsers.GUESTS 的共用訪客,--target 下所有 class 共用它 (#642, 2026-09-22, verifier@opus)」
  3. (選配)→ tabby_pool 專案層:#642 時序窗口的數字原文「150ms 在負載下開不出窗口(前提大聲紅),600ms 兩邊都穩 —— 比照 #639 的 T1 (#642, 2026-09-22, verifier@opus)」;角色卡只留原則。
- 這次討論教了誰什麼:
  - opus:先 `grep -n` 再寫行號(第 1 輪行號錯了 5 處);「主檔已有」不等於「沒用」,要查範本有寫時錯誤是否仍發生(#25)。
  - fable:(待 fable 填)上限估算要把三標記的字元算進去。
