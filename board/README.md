控制台。`python3 board/board.py`(預設 127.0.0.1:18905)。只讀 `tickets/*.json` 與 `board/events.jsonl`,不猜。
設定 `board/config.json`:埠、票目錄、事件檔、模型路由、記憶門檻、健康檢查 URL(可空)。
畫面:票與依賴 / agent 時間線 / 落地佇列 / 決策收件匣 / 「哪些保證還只在演練裡成立」。
